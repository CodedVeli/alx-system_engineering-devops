#!/bin/bash

# Set the initial count to zero
count=0

# Loop through all directories (including hidden ones) in the current directory
for dir in $(find . -type d -not -path '*/\.*' -not -path '.' -not -path '..'); do
  # Increment the count for each directory
  count=$((count+1))
done

# Output the count
echo $count

